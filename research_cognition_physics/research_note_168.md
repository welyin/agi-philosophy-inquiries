# 第一百六十八轮：在四角度恢复之外加入相干关系修正

日期：2026-09-16。接续第167轮，扩大实际恢复操作，而非继续微调已经接近该类最优的四个角度。

**结果：找到一个精确实正交的扩展方向。它只需在所有报告的恢复操作后加入同一组相干关系旋转，就能离开旧四角度类别；两种Y报告也随之改变。所有读取和消息规则保持。本轮仅提出并验证合法候选，全局改善由第169轮另证。**

## 1 扩展恢复器

沿用第165轮四个固定有理角度，记原负取向误差块为W_rg=Ũ_gᵀU_r。定义实反对称矩阵

\[
J=\begin{pmatrix}0&-1\\1&0\end{pmatrix},\qquad
G=Z_R\otimes X_A\otimes J_B,\qquad
H=Z_R\otimes J_A\otimes Z_B.
\]

直接矩阵乘法给出

\[
G^T=-G,\ H^T=-H,\ G^2=H^2=-I_8,\ GH=HG=I_R\otimes Z_A\otimes X_B.
\]

因此可取共同相干修正

\[
E(\varepsilon)=e^{-\varepsilon G}e^{\varepsilon H}
=(cI_8-sG)(cI_8+sH),\qquad c=\cos\varepsilon,\ s=\sin\varepsilon.
\]

本轮固定tan(ε/2)=3/800，ε约0.007499964844弧度；c、s及E均为精确有理矩阵。完整取向操作是diag(I₈,E)，Q保持相干且不被读取。

令T为原取向坐标变换，则新解码器为

\[
D_g^{\rm new}=T^T\operatorname{diag}(I_8,E\widetilde U_g^T),\qquad
A_{rg}^{\rm new}=T^T\operatorname{diag}(I_8,EW_{rg})T.
\]

E相同，不需要多判断一个未知标签；两种Y报告原先固定的负块也被E改变，所以新方案已不属于第167轮的四角度类别。它仍只访问已有Q、R、A、B，不读取封闭源纯化或噪声浴。

## 2 用微积分寻找类别之外的方向

E在ε=0的导数是H−G。在一个固定来源处，沿用Schur根生成的接触系数a_rg，令

\[
D=I_8-\sum_{r,g}a_{rg}EW_{rg},\qquad
D'=-\sum_{r,g}a_{rg}(H-G)EW_{rg}.
\]

则误差对ε的导数为

\[
\frac{\operatorname{Tr}[\sigma\operatorname{Tr}_R(D^TD'+D'^TD)]}{4d},
\qquad d=1-\sum_{r,g}a_{rg}^2/p_{rg}.
\]

在第166轮旧合法见证处，ε=0的数值导数约−0.06858316，显示一个显著的局部下降方向。解析式与有限差分核对，但它只说明这个固定输入的变化，不能直接证明对全部未知输入取最坏后的误差下降。

## 3 最坏输入可能随操作改变

多起点搜索对每个候选重新寻找两方未知输入，而不是一直跟随旧见证。结果有明显的分支变化：

| tan(ε/2) | 数值最坏误差候选 | 候选中两方Y坐标乘积 |
|:---|---:|:---|
| 0 | 0.449394739421 | 负 |
| 3/2000 | 0.449202270453 | 负 |
| 1/400 | 0.449166631011 | 正 |
| **3/800** | **0.449153625938** | **正** |
| 1/200 | 0.449162619258 | 正 |
| 1/100 | 0.449405567270 | 正 |

表中是数值探索，不是全局最坏值证书，也没有认证精确切换点。它说明沿旧输入的负梯度不断加大修正并不可靠：其他输入会成为新的困难情形，较大修正甚至可能更差。

## 4 完整未知外部关联仍受检验

E及各W在R上仍为块对角，只含I、Z。因此旧约简继续成立：整个恢复通道的纯化误差只依赖σ=Re(ρ_A⊗ρ_B*)。36分支及目标列构成37维Gram，和直接物理纯化输出逐例核对。

这里J是实矩阵生成元。其原门实现使用已有Ry和YX，不是新增Y方向读取，也没有假定主体能无扰读出自身未知状态。源、记录、指针与浴仍在整体中；恢复器的访问权限没有扩大到封闭环境。

## 5 复算与下一步

- [实现与7项检查](relational_history_decoder.py)、[结果](relational_history_decoder_results.json)。
- [可选多起点搜索](search_relational_history.py)、[分支探索记录](relational_history_search_diagnostic.json)。候选探索用已有SciPy，默认检查不用优化器。
- [新的合法来源及有理测试向量系数](relational_history_witness.json)。

下一轮须证明新统一上界低于旧四角度族共同下界0.4493947394，才能宣布突破整个旧类别。原门成本随后单独核算，不沿用413门的旧数字。
