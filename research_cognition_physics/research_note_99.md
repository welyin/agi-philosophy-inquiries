# 第九十九轮：关联中保留的，是以后选择查询的能力

日期：2026-09-16。接续 [第 98 轮](research_note_98.md)。

## 1 本轮结果

第一步写入对第二份记忆造成的可读衰减，并不代表相应信息全部消失。**两份记忆的关联保留了它。**

仍使用第 97 轮相同的两次写入。只访问 M,N：

\[
D_G=\sqrt{1-c^2}=v,\qquad D_H=\sqrt{1-d^2}=w.
\]

这里 D_G、D_H 是两套等先验本征态校准任务各自的最优记忆迹距离。与只看 N 的 c w 相比，联合记忆恢复了完整 w。

实门能够有限步读取这项关联。因此同一套未读的存储，在之后选择 G 或 H 时，分别达到

\[
P_G^{(m)}=\frac{1+\gamma_m v}{2},\qquad
P_H^{(m)}=\frac{1+\gamma_m w}{2}.
\]

**每次运行选择其中一个查询。** 这不提供同时取得两个各自最优答案的办法；相干存储的查询选择权，与已经输出的两个经典答案必须分开核验。

## 2 从完整记忆态直接求界

对一组条件记忆 m_±(c)，定义

\[
A_c=|m_-\rangle\langle m_-|+|m_+\rangle\langle m_+|,
\]

\[
B_c=|m_-\rangle\langle m_+|+|m_+\rangle\langle m_-|,
\qquad
D_c=|m_+\rangle\langle m_+|-|m_-\rangle\langle m_-|.
\]

对第 98 轮 G、H 校准输入，实际两记忆约化态分别为

\[
\rho^G_s=|m_s(c)\rangle\langle m_s(c)|\otimes\frac{A_d}{2},
\]

\[
\boxed{\rho^H_t=\frac{A_c\otimes A_d+tB_c\otimes D_d}{4}.}
\]

第一式说明 G 信息保存在 M，加入 N 没有额外提高它的最优区分度。

第二式中，B_c 的本征值为 c±1，故 ||B_c||₁=2；D_d 的本征值为 ±w，故 ||D_d||₁=2w。因此

\[
\frac12\|\rho^H_+-\rho^H_-\|_1
=\frac14\|B_c\|_1\|D_d\|_1=w.
\]

若忽略 M，只剩 tr B_c=2c，才得到 c w。信息差额来自联合关联，没有增加一次对旧系统的写入。

## 3 两种具体查询

记

\[
N_c=cX-vZ,\qquad L_c=cZ+vX,\qquad N_d=dX-wZ.
\]

两个最优理想记忆查询为

\[
O_G=N_c\otimes I,\qquad
\boxed{O_H=L_c\otimes N_d.}
\]

它们不仅对校准输入奏效；完整等距映射满足

\[
V^\dagger(O_G\otimes I_{\mathrm{old}})V=vG,\qquad
V^\dagger(O_H\otimes I_{\mathrm{old}})V=wH.
\]

所以对任意旧输入及旁观系统，它们分别实现相应无偏弱效应。

G 查询沿用第一份记忆的原读取轴。H 查询有两条实现路线：

### 共同实门读取

1. 在 M 上旋转 −arccos c，使 L_c 对齐 Z。
2. 在 N 上旋转 −[π/2+arccos d]，使 N_d 对齐 Z。
3. 使用原受控 R_y(π) 门，把 Z_M Z_N 的乘积值转为 M 的 Z。
4. 原噪声读取 M；若需要保护式重复，沿用原指针流程。

这段解码共 **1 次两系统旋转、5 次局部旋转**。没有 SWAP 或新理想测量。加上写入，单次 H 查询共 5 次两系统旋转、11 次额外局部旋转、2 个记忆槽和 1 次原读取。单次 G 查询则为 4、6、2、1。

两记忆共同解码后读取只作用于新增存储，不额外改变求和后的旧约化态；条件旧态可以改变。程序也核对保留复参考的任意相关输入。

### 分开后局部读取加通信

若两个记忆已分开，可分别读取 M 的 L_c 和 N 的 N_d，将两个符号相乘。各读一次时成功率为

\[
P_H^{\mathrm{local}}=\frac{1+\alpha^2w}{2}.
\]

需两次原局部读取和一个结果位的通信，无需再次共同量子门。两人读的都是为 H 选择的轴；第一方没有在同一过程中获得原 G 查询。

这条路线的统计可用 γ_mγ_n 替代 α²，但分布式保护指针的准备和重置应分别计入，不能默认共用一个无需传送的辅助位。

## 4 数值例子

c=d=4/5，对旧态的最坏扰动仍为 0.19：

| 选定的任务与读取方式 | 原读取次数 | 成功率 |
|:---|:---:|:---:|
| 查询 G，读取 M | 1 | 79.6884751105% |
| 查询 H，只读取 N | 1 | 73.7507800884% |
| 查询 H，两份局部读取并通信 | 2 | 79.3801851463% |
| 查询 H，共同实门解码后读取 | 1 | 79.6884751105% |
| 查询 H，共同解码后保护式读取 | 3 | 79.9951644082% |

共同读取相对只看 N 的提升，有严格有理证书：单次提升约 5.9376950221 个百分点。不同实现的门数及通信不同，未声称相同全部资源下最优。

强写入 c=d=0 时区别更明显：N 单独对 H 完全无信息，成功率 1/2；两份关联查询一次原读取却达约 99.4807918509%。代价包括更大的旧最坏扰动 3/4，不能把这个强例子与 Δ=0.19 的弱例子混为同一预算。

## 5 为什么不能把两种最优查询同时兑现

L_c 与 N_c 是正交的 Pauli 方向，因此

\[
O_GO_H+O_HO_G=0.
\]

更具体地，当 c<1、d<1 时，两套校准的差矩阵均可逆：

\[
\rho^G_+-\rho^G_-=\frac{D_c\otimes A_d}{2},\qquad
\rho^H_+-\rho^H_-=\frac{B_c\otimes D_d}{2}.
\]

二态等先验最优辨认的正效应，必须是各差矩阵的正谱投影；可逆性使它唯一。上述最优投影分别为 (I+O_G)/2、(I+O_H)/2。

若两个投影 P、Q 来自一个联合正效应族，则每个细结果 F≤P 或 F≤I−P，必须支持在相应子空间。对细结果求和得到 Q，故 Q 与 P 交换。这里两个投影不交换，所以**固定这套已写入记忆，不能在一次运行中同时达到两个各自的理想最优成功率**。

这比仅引用第 98 轮原效应圆盘更具体：即使弱强度 v²+w²≤1，换一种写入仪器可能存在兼容原效应，也不代表当前存储能同时实现两个各自唯一的最优分析。有限噪声下的完整联合精度前沿仍未求解。

## 6 强写入时，逻辑二能级结构进入记忆关联

c=d=0 时，

\[
O_G=-ZI,\quad O_H=-XZ,\quad O_K=-iO_GO_H=YZ.
\]

对任意旧四维复态 ρ，令 g=tr(Gρ)、h=tr(Hρ)、k=tr(Kρ)，实际两记忆约化态为

\[
\boxed{\rho_{MN}=\frac{I-gZI-hXZ+kYZ}{4}.}
\]

这三个编码算子平方为 I、两两反对易，保存一个逻辑二能级系统的关系代数。若输入含复相干，k 可以非零，并保存在记忆的复关联中；原实效应并不因此自动拥有直接读取 k 的权限。

公式只涉及旧态的三个逻辑坐标，不能称为完整复制任意四维旧态。旧态其他自由度及其关联仍要在更大整体中核验；完整写入酉门保持整体可逆，也不等于每个约化部分独自包含全部信息。

这给“整体还在”提供了一个明确实例：局部第二份记忆看不到的信息，可能保存在联合关联；以后仍能选择恢复哪种查询。实候选同样容许本轮 G、H 的查询能力，故这仍不是复量子数域的认知推导。

## 7 验证、归属和下一步

[脚本](deferred_relation_queries.py)及[结果](deferred_relation_queries_results.json)：新增 8 项检查，全部通过。涵盖两套完整校准态、精确迹范数式、全部输入的查询效应、实际解码门、局部通信效应、有限 Kraus 记录及复参考、唯一最优投影、强写入逻辑代数。

二态谱投影的最优性沿用之前已核对的 [Watrous 第 3 章，Theorem 3.4](https://cs.uwaterloo.ca/~watrous/TQI/TQI.3.pdf)。本轮新增的是当前顺序写入的记忆态、关联查询、原实门及噪声成本核对；没有声称首次发现量子互补或延迟选择现象。

**下一主任务**：让第一份记忆在原 G 读取轴上发生可调退相干，或被读成经典记录。保持旧写入不变，求关联 H 查询能力随记忆相干损失的精确变化，并给出实际有限读取与成本。由此检查“保留旧答案”是否足以保留以后扩展查询的能力。
