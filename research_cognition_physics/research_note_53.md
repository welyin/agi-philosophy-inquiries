# 第五十三轮：全部 fresh 操作扩展的精确分类与替换误差

日期：2026-09-15。接续第 52 轮补出的操作标量 κ。

## 1 结论

固定原 α=4sin(1/4)，保持全部原单系统 fresh 输入输出和每个结果概率不变。在**实 2×2 Kraus 操作、通常实张量组合**的范围内，每个结果 s 的全部合法扩展恰好为

\[
\boxed{\frac{(2\alpha-1)\sqrt{1-\eta^2}}2
\le\kappa_s\le\frac{\sqrt{1-\eta^2}}2.}
\]

两结果可独立选取各自区间内的值，各读取方向的区间相同。η<1 时都有非零宽度；η=1 时 κ_s=0，完整操作扩展唯一。

若两个实操作的局部矩阵 B 相同，则它们在任意实辅助系统、任意联合输入下的最大输出差异精确为

\[
\boxed{\sup_{d,\rho}\frac12\|[(\Phi-\Psi)\otimes\mathrm{id}_d](\rho)\|_1
=\frac{|\kappa_\Phi-\kappa_\Psi|}{2}.}
\]

一个实辅助二能级系统就能达到上界。这是当前模型内的完整分类与误差界，不是一般概率理论分类，也不是从认知选择了 κ 的某个值。

## 2 先解决非选择噪声的全部扩展

设实噪声映射在局部校准下满足

\[
I\mapsto I,\quad X\mapsto\alpha X,\quad Z\mapsto\alpha Z.
\]

第 52 轮证明其余作用只有 `Y→γY` 一个自由数。故记为 D_γ。

将 D_γ 作用于实 Bell 输入 `|Φ⁺⟩=(|00⟩+|11⟩)/√2` 的第一边，输出为

\[
C_\gamma=\frac14(II+\alpha XX-\gamma YY+\alpha ZZ).
\]

在 Bell 基底上，它的四个特征值为

\[
p_I=\frac{1+2\alpha+\gamma}{4},\quad
p_X=p_Z=\frac{1-\gamma}{4},\quad
p_J=\frac{1-2\alpha+\gamma}{4}.
\]

联合输出必须为正，所以必要条件是

\[
\boxed{2\alpha-1\le\gamma\le1.}
\]

反过来，在该区间中四个 p 都非负且和为 1，用实 Kraus 矩阵

\[
\sqrt{p_I}I,\quad\sqrt{p_X}X,\quad
\sqrt{p_J}J,\quad\sqrt{p_Z}Z,\qquad J=-iY,
\]

直接构造 D_γ。因此该区间同时必要且充分，没有遗漏其他实 Kraus 扩展。

第 46—52 轮的原扩展位于 γ=1 上端，各向同性扩展位于 γ=α；此前的两个例子只是这条完整区间上的两个点。

代码以有理示例 α=3/5 交叉核对两侧排除：γ=1/10 或 11/10 都产生精确特征值 −1/40。它只作通用代数对照，没有替换项目固定 α。

## 3 还原到每个 fresh 分支

沿 Z 的结果 s=±1 对应实过滤矩阵

\[
L_s=\operatorname{diag}\left(\sqrt{\frac{1+s\eta}{2}},
\sqrt{\frac{1-s\eta}{2}}\right),\qquad
\det L_s=\frac{\sqrt{1-\eta^2}}2=:c_\eta.
\]

一般方向是将 L_s 作实旋转共轭，不改变行列式。全部候选为

\[
\Phi_{s,\gamma_s}(M)=L_s\mathcal D_{\gamma_s}(M)L_s^T.
\]

其局部作用与原分支完全一致，而

\[
\kappa_s=c_\eta\gamma_s.
\]

**没有其他扩展的证明。** 当 η<1 时 L_s 可逆。对任一与原分支局部等价的实 Kraus 映射 Φ_s，在输出两侧乘 L_s⁻¹ 和 L_s⁻ᵀ，得到另一个实 Kraus 映射；它在 I、X、Z 上恰好是上述噪声作用。因此必属于 D_γ 的已证明区间。这个逆过滤只用于代数证明，没有假设它是一项概率守恒的物理操作。

两结果的效应 `Φ_s*(I)` 均固定为原效应，不依赖 γ_s；其和仍为 I。所以两个 γ_s 可以独立选择，完整仪器继续归一。

## 4 为什么 η=1 例外

η=1 时 L_s 为秩一矩阵，不能继续使用上面的逆矩阵论证。

此时原分支对每个纯输入的输出，都支撑在同一个固定一维子空间。若 `Φ_s=Σ_jK_j(·)K_jᵀ` 局部等价，由各项正性，任何 K_j 都不能把任何实输入向量送到这个子空间以外：对输出正交方向取矩阵元，得到一组平方之和为零，故每项都为零。

所以每个 K_j 的秩至多为 1，`det K_j=0`，进而 **κ_s=0**。原局部矩阵再加 κ 已经决定全部作用，故扩展唯一。

这里 η=1 是原 fresh 仪器的最大强度。由于前置噪声 α<1，实际读取效应仍不是投影读取；唯一性来自分支输出的秩一支撑，不应称作已经得到无噪声测量。

## 5 区间有多宽

每个分支的区间宽度是

\[
\Delta\kappa=(1-\alpha)\sqrt{1-\eta^2}.
\]

| 原强度 η | κ_s 下端约值 | κ_s 上端约值 | 宽度约值 |
|---:|---:|---:|---:|
| 0 | 0.4896158370 | 0.5000000000 | 0.0103841630 |
| 0.5 | 0.4240197530 | 0.4330127019 | 0.0089929489 |
| 0.85 | 0.2579211465 | 0.2633913438 | 0.0054701973 |
| 1 | 0 | 0 | 0 |

表中小数用于阅读；结果文件另给 100 位区间与宽度的严格有理下界。η=0 依然保留原 fresh 噪声，不能把这行误当作恒等操作。

对 ρ₊ 探针执行分支后，再用第 52 轮共同门和原 η=1 读取，保留完整的 `(s,r)` 记录：

\[
p(s,r)=\frac14+\frac{r\alpha\kappa_s}{2}.
\]

两个结果同时选择区间上端与同时选择下端时，完整记录的总变差距离恰为

\[
\boxed{\alpha(1-\alpha)\sqrt{1-\eta^2}.}
\]

这给出一项直接可执行的区间两端区分协议。

## 6 任意辅助大小下的精确误差界

令 δ=κ_Φ−κ_Ψ，两操作 B 相同。它们在所有矩阵上的差满足

\[
(\Phi-\Psi)(M)=\frac\delta2\operatorname{Tr}(YM)Y.
\]

对任意归一化联合状态 ρ，令

\[
C=\operatorname{Tr}_A[(Y\otimes I)\rho].
\]

C 是 Hermitian 矩阵。对任意 `||H||≤1` 的 Hermitian H，有

\[
|\operatorname{Tr}(HC)|=|\operatorname{Tr}[\rho(Y\otimes H)]|\le1,
\]

故 `||C||₁≤1`。于是

\[
[(\Phi-\Psi)\otimes\mathrm{id}](\rho)=\frac\delta2Y\otimes C,
\]

\[
\frac12\|[(\Phi-\Psi)\otimes\mathrm{id}](\rho)\|_1
=\frac{|\delta|}{2}\|C\|_1\le\frac{|\delta|}{2}.
\]

用实两系统输入 `ρ₊=(I+YY)/4`，有 C=Y/2、`||C||₁=1`，取得等号。证明不要求辅助维数有固定上限。

对于选择性操作，两输出保留未归一化总质量；局部 B 相同保证失败概率也相同。对相应完整仪器应用此界，需逐分支保留结果，不能先任意归一化后套用。

因此，局部矩阵已校准而 κ 的误差至多 ε 时，任意联合使用的一次输出误差至多 ε/2。这使第 52 轮的精确替换判据具有定量版本。后续合法操作不会放大迹距离；若只看原带噪声探针，实际可读距离还乘以 α。

## 7 已解决什么，仍缺什么

本轮把“局部更新有多种扩展”从例子变成了必要且充分的分类，并完成任意辅助维数的严格误差界。当前实候选的操作描述可以通过联合校准补齐，因而“接口必须可预测地组合”本身仍不足以迫使复量子结构。

这里 κ 的不唯一性与第 21 轮准备非情境性不同：没有新增隐藏准备分布约束，也没有更改已完成的 η_B 阈值。

下一步应转向一个能带来结构变化的条件性问题：**如果坚持让 YY 联合区别通过各主体分别读取而可见，必须新增哪种局部读取？新增读取与条件准备闭包会迫使本地状态空间怎样扩展？**

先在当前矩阵候选中审计最小新增方向，并检验对实联合纯态的一侧条件读取会产生哪些远端状态。明确区分“加入某读取后迫使更大状态空间”与“已从认知证明必须加入该读取”。这个入口开始分析复结构的可能来源，而非继续只优化实控制成本。

## 8 文献与复核

沿用第 52 轮已读的 [Leung，Choi’s Proof and Quantum Process Tomography](https://arxiv.org/pdf/quant-ph/0201119) §2—4，作为辅助探针与完全正性框架的来源。本轮必要性直接计算实 Bell 输入的四个输出特征值，充分性给出实 Kraus 矩阵；fresh 的逐分支完整区间、η=1 的秩一证明及 κ 的精确误差界都在上文展开。未声称 Kraus/Choi 理论或 Pauli 通道表示为本项目原创。

实现：[fresh_extension_classification.py](fresh_extension_classification.py)。结果：[fresh_extension_classification_results.json](fresh_extension_classification_results.json)。

```text
python -X utf8 research_cognition_physics/fresh_extension_classification.py --write-results
python -X utf8 -m unittest discover -s research_cognition_physics -p "*.py"
```

新增 **12 项检查通过**：全部区间构造、Bell 特征值、两侧有理负值、原分支逐基对照、逆过滤、η=1 的唯一扩展、独立选择结果扩展、联合输入的上界与饱和、完整记录距离、顺序组合与严格区间。Python 3.12.14、NumPy 2.3.5，无新增依赖；旧轮次代码与结果保留。

完成第 53 轮后，目录全部 **591 项检查通过**，完整回归耗时 109.571 秒。第 50—53 轮共新增 47 项检查及四组独立代码、结果和研究笔记。
