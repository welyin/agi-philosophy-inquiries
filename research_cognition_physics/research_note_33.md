# 第三十三轮：一次齐次凸见证已用尽，端点化为半圆耦合

日期：2026-09-15。接续第三十二轮的严格下界 0.1182。保持原准备宽度、fresh 族和全部精确旋转。

## 1 本轮解决什么

一般可行强度仍在 **[0.1182,η_B]**，η_B≈0.144739232095。本轮没有宣布新的数值上界，而是证明两件能缩小后续搜索范围的事实：

1. 对第二十九轮的固定圆周编码，**全部有限值、凸、正一次齐次函数**的测试，都等价于已经得到的 αG(η)≤1。继续更换范数、中心投影方向或齐次多面体函数，不能把上界压得更低。
2. 若 η_B 这个单一读取强度有任意准备非情境实现，则它必能投影为固定圆周编码；该端点的存在性进一步等价于**两个指定正半圆分布之间的凸序**。

同时得到任意合法固定编码耦合的加权跨半圆转移量恒等式。它量化了趋近端点时必须消失的一种隐藏转移。

“正一次齐次”指 φ(tv)=tφ(v)，t≥0；这里“有限值”不是“只测有限多个函数”。

## 2 任意方向的绝对投影可以精确积分

仍记 X 为均匀单位圆上的向量，Y=αn_ψ，ψ 的密度为 R_ηdψ/(2π)。对任意方向 n_θ，利用原结果—输出角换元：

\[
\begin{aligned}
E|Y\cdot n_\theta|
&=\frac\alpha2\int\left(
|A\cos(y-\gamma)+b|+
|A\cos(y-\gamma)-b|
\right)\frac{dy}{2\pi}\\
&=\alpha\int\max\{|A\cos y|,b\}\frac{dy}{2\pi},
\end{aligned}
\]

其中 b=η|cos θ|，A²=cos²θ+q²sin²θ=q²+b²，q=√(1−η²)，γ 只是把正弦和余弦合并后的相位。

积分给出

\[
\boxed{
E|Y\cdot n_\theta|
=\frac{2\alpha}{\pi}
\left[q+b\arctan\frac bq\right].
}
\]

X 的对应平均恒为 2/π。函数 b↦q+b arctan(b/q) 严格随 b>0 增加，而 0≤b≤η，所以最大值在读取轴 θ=0 mod π 处取得：

\[
q+\eta\arctan(\eta/q)=q+\eta\arcsin\eta=G(\eta).
\]

因此所有中心绝对投影测试通过，当且仅当 αG(η)≤1。该结论是全方向解析结果，不是方向网格的最大值猜测。

## 3 为什么其他一次齐次凸函数也不能更强

两个待比较分布均中心对称。对任意一次齐次凸函数 φ，换成

\[
\phi_{\rm even}(v)=\frac{\phi(v)+\phi(-v)}2
\]

不会改变两边期望。偶的一次齐次凸函数是一个中心对称紧凸集的支撑函数；在二维，它有正测度表示

\[
\phi_{\rm even}(v)=\int|\langle v,n_\theta\rangle|\,m(d\theta),
\qquad m\ge0.
\]

这可从中心对称多边形理解：它的边成对平行且等长，因此可由线段作 Minkowski 求和；支撑函数相应是绝对线性函数的正和。用这样的多边形逼近一般平面中心对称凸集，即得积分表示；线段等退化情况也包含在极限中。

该标准平面表示可在 Christoph Haberl 的论文预备部分、式 (12) 附近核对。[Complex affine isoperimetric inequalities，PDF 第 5 页](https://www.dmg.tuwien.ac.at/haberl/ComplexAffineInequ.pdf)。本轮使用的是该表示，不是论文的复仿射等周不等式。

于是

\[
E\phi(X)-E\phi(Y)
=\int\frac2\pi
\left[1-\alpha\left(q+b_\theta\arctan\frac{b_\theta}q\right)\right]m(d\theta).
\]

若 αG≤1，右侧每项非负；若 αG>1，单个 |v_x| 已经违反。故

\[
\boxed{
E\phi(Y)\le E\phi(X)\quad
\text{对全部一次齐次凸 }\phi
\iff\alpha G(\eta)\le1.
}
\]

这只穷尽了一个函数类别，不能替代第二十九轮要求的**全部凸函数**。尤其是带非零截距的最大仿射函数，一般不在此类中。

一个独立控制例说明逻辑差别：在横轴上取 μ=(δ_{−2}+δ_2)/4+δ_0/2，ν=(δ_{−1}+δ_1)/2。全部一次齐次函数在两者的期望相同，但 φ(x,y)=(|x|−1)_+ 在 μ 上为 1/2、在 ν 上为零。这不是本模型的反例，只用于防止把函数类别的通过误读为完整凸序。

## 4 端点上，任意编码都能投影到固定角度编码

第三十轮已证明：如果 η=η_B 有准备非情境实现，任意一般编码

\[
d\mu_t(\lambda)=[1+a(\lambda)\cdot t]d\mu_0(\lambda)
\]

必满足 |a|=1 几乎处处，而且 a 的基准分布是均匀单位圆。

现在去掉额外隐藏标签，而不作输入状态相关的重新采样。将均匀输入下的联合分支测度 μ_0(dλ)K_s(dλ′|λ) 直接推送到 (a(λ),a(λ′))，得到圆×圆上的 M_s(dx,dy)。其合计输入边缘为 σ(dx)，故可分解为

\[
M_s(dx,dy)=\sigma(dx)\widehat K_s(dy\mid x).
\]

一般输入仅将原联合测度乘以 1+n_x·t，这个因子只依赖已经保留的输入角。回到 z=(w,u,v) 坐标，新的角度核满足

\[
\widehat K_s F_z=F_{A_sz}
\]

作为完整分布恒等式。静默旋转直接作用于角度即可。这里只对紧圆周上的联合分布取条件概率，不需要假设原隐藏变量纤维具有特殊结构。

反方向显然：固定角度模型本身就是一般模型的一种。因此，**在 η_B 这个单一强度上**，任意编码存在性与固定编码存在性等价。低于端点时 |a|=1 尚未推出，不能把这次投影证明推广到所有强度。

## 5 精确的跨半圆转移量

对任何固定编码的合法耦合 E[X|Y]=Y，有

\[
\begin{aligned}
E|X_x|-E|Y_x|
&=E[|X_x|-\operatorname{sign}(Y_x)X_x]\\
&=2E\!\left[|X_x|\,\mathbf1_{\{X_xY_x<0\}}\right].
\end{aligned}
\]

Y_x=0 的事件概率为零，所以无需在边界加额外项。代入第二节：

\[
\boxed{
E\!\left[|X_x|\,\mathbf1_{\{X_xY_x<0\}}\right]
=\frac{1-\alpha G(\eta)}{\pi}.
}
\]

这是按 |X_x| 加权的跨半圆转移量，不能直接称为跨半圆概率。它在 η=0.1182 时约为 0.00110230719，在 η=0.14 时约为 0.000213265511；到 η_B 必须为零。若只计 |X_x|≥ε 的跨越，其概率至多为右侧除以 ε，但靠近分界线的无权重概率不受这个估计直接控制。

第三十二轮的显式核通过直接积分其四个原子和连续项，独立核对了该恒等式。

在端点，X、Y 必须处于相同的正/负半平面。因此任何对相反半圆仍给予正联合质量的候选核，都不能达到 η_B。特别地，仍含正质量的全圆均匀重置项不能达到端点；这并不排除它在端点以下任意接近，也不单独给出更低的一般数值上界。

## 6 将端点问题缩为正半圆凸序

定义概率分布

\[
\sigma_+=\operatorname{Law}(n_x\mid \cos x>0),\qquad
\nu_+=\operatorname{Law}(\alpha n_\psi\mid \cos\psi>0),
\]

其中 σ_+ 是均匀正半圆；ν_+ 的角密度为 R(ψ)dψ/π，−π/2<ψ<π/2。

在端点，两者的均值恰好相同：

\[
E_{\sigma_+}X=(2/\pi,0),\qquad
E_{\nu_+}Y=(2\alpha G(\eta_B)/\pi,0)=(2/\pi,0).
\]

若全圆鞅耦合存在，第五节说明它不跨半圆，将其限制并归一化就得到正半圆鞅耦合。反之，若有正半圆耦合，把它关于原点反射，再各取一半，便得到完整耦合。因此利用第二十九轮的 Strassen 判据，

\[
\boxed{
\eta_B\text{ 单一读取强度有任意准备非情境实现}
\iff \nu_+\le_{\rm cx}\sigma_+.
}
\]

当前没有证明右侧成立或失败。这个化简的价值是：额外隐藏标签与任意系数半径已不再是端点搜索中的自由度，接下来可以专门研究两个明确半圆分布之间的正耦合。

注意：即使单一强度 η_B 最终被构造出来，也还需要实现全部较低强度，才能称整个 [0,η_B] 仪器族可行。本轮没有凭端点存在性偷渡整族存在性。

## 7 非齐次扫描与下一项任务

代码另探索了 φ(v)=(|v·n_θ|−k)_+：33 个方向、50 个截距，使用 8192 个角积分点，在 η=0.1182、0.14、η_B 上扫描。这组有限扫描没有找到负值；端点的最小值约 6.45×10^−10，对应 k=0 的理论零点，不能解释为严格正余量。

这些结果既未验证所有截距与方向，更未验证所有二维凸函数。下一项明确任务是：**构造正半圆鞅耦合，或从带截距的多面体凸函数中找出严格反例**。若获得固定编码的非端点反例，仍须单独处理其对任意编码的推广；只有本轮证明的端点投影可以直接使用。

## 8 复算和文献边界

代码：[homogeneous_witness_boundary.py](homogeneous_witness_boundary.py)。结果：[homogeneous_witness_boundary_results.json](homogeneous_witness_boundary_results.json)。9 项检查通过，包含任意方向的两种独立积分、齐次函数正和与对称化、端点半圆均值、显式核的跨半圆转移量，以及齐次测试不等于完整凸序的控制例。

~~~
python -X utf8 research_cognition_physics/homogeneous_witness_boundary.py --write-results
~~~

本轮针对性核对 Haberl 论文的支撑函数预备定义与式 (12) 附近的平面表示，没有声称复现该文主定理。标准凸序耦合定理来源与阅读范围见第二十九轮；本模型的方向积分、测试类别边界、端点投影和跨半圆恒等式另行推导。

新增准备非情境假设仍未从认知原则推出；第七轮 η=0.5 的一般横向保留区间 [0.8570324548,0.8660254038] 仍为独立未解任务。本轮没有推出量子理论或引力。
