# 第一百四十三轮：相干历史退相干后的最优恢复误差

日期：2026-09-16。完整经典化与完整相干保存之间，还需检查有限存储质量。

**结果：若 n−1 个取向标记各自受到独立退相干，保留因子为 λ_j∈[0,1]，全部旧接入历史仍可相干访问，但新增噪声环境不可访问，则任意恢复通道的最优最坏外部误差为 1−∏_j(1+λ_j)/2。**

这里声明的是新的存储噪声模型，λ 不等于原读取强度 η。没有在噪声发生前增加冗余纠错编码。

## 1 明确的噪声与完整实现

在完整取向重编码之后，每个标记位受到

\[
\mathcal D_{\lambda_j}(\omega)
=\tfrac{1+\lambda_j}{2}\omega
+\tfrac{1-\lambda_j}{2}Z_j\omega Z_j.
\]

标记的对角块不变，非对角块乘以 λ_j。独立噪声使两个取向 b,c 之间的块乘以所有不同位的 λ_j 之积。

两主体情形可直接加在第 134 轮实际七位电路之后：新增一个纯浴位，在 h=1 时对它执行 Ry(2 arccos λ)，h=0 时不动。两种浴状态的重叠恰为 λ。此受控旋转已有原门编译，需一个 YX、三个局部旋转和一个纯浴位。

该模型中浴位保留在整体，但不对恢复器开放。旧 h、R、A、B、E_1、E_2、C 则全都可访问。代码核对实际八位扩张，逆转旧接入后恰好得到取向退相干；不是凭空对逻辑状态加入一个公式。

## 2 逆转旧接入后剩余的通道

沿用第 142 轮的实反射 Q_a=Σ_b(−1)^(a·b)P_b，得到

\[
\mathcal N(\omega)=\sum_a p_aQ_a\omega Q_a,
\qquad
p_a=\prod_j\frac{1+(-1)^{a_j}\lambda_j}{2}.
\]

这些 Q_a 满足 Tr(Q_a^T Q_b)=d δ_ab，其中 d=4^n；Q_0=I。由于 λ_j≥0，最大权重为

\[
p_{\max}=p_0=\prod_j\frac{1+\lambda_j}{2}.
\]

对两主体实际电路，Z_h V=VQ_1，所以噪声输出仍位于原接入等距映射 V 的像空间。允许恢复器操作全部旧七位系统，与允许对逆转后的原输入作任意通道同样一般，下面的界没有把控制器人为限定为“只会逆转”。

一般 n 使用明确取向表示；未声称编译了全部 n 方物理电路。

## 3 任意后处理不能提高最大纠缠重叠

令恢复通道的 Kraus 算子为 R_l。目标重叠为

\[
F_\Phi=\frac1{d^2}\sum_{l,a}p_a|\operatorname{Tr}(R_lQ_a)|^2.
\]

Q_a/√d 在矩阵 Hilbert–Schmidt 内积下正交。Bessel 不等式给出

\[
\sum_a|\operatorname{Tr}(R_lQ_a)|^2
\le d\operatorname{Tr}(R_l^\dagger R_l).
\]

因此

\[
F_\Phi\le\frac{p_0}{d}\sum_l\operatorname{Tr}(R_l^\dagger R_l)=p_0,
\qquad D\ge1-p_0.
\]

这覆盖全部实或复 CPTP 恢复和独立辅助，不靠随机搜索决定最优。

## 4 简单逆转达到锐界

逆转旧接入后不再修改原系统，通道中恒等项的权重为 p_0。对任意外部联合态，误差至多 1−p_0。最大纠缠输入将各个 Q_a 变成正交向量，恰好达到。

于是

\[
\boxed{\epsilon_{\rm ext}^{*}=1-\prod_{j=1}^{n-1}\frac{1+\lambda_j}{2}}.
\]

相同 λ 时，结果为 1−[(1+λ)/2]^(n−1)：

| λ | 两主体 | 三主体 | 五主体 |
|:---|---:|---:|---:|
| 0 | 1/2 | 3/4 | 15/16 |
| 9/10 | 0.05 | 0.0975 | 0.18549375 |
| 99/100 | 0.005 | 0.009975 | 0.019850499375 |
| 1 | 0 | 0 | 0 |

λ=0 回到完整取向去相干的误差，λ=1 为完整恢复。一般 0≤λ≤1 的承诺独立输入边缘都保持不变，因此只核对主体自身状态，无法发现这些外部关系误差。

## 5 维护要求与边界

若 n>1、要求外部误差不超过 ε，且使用相同 λ，则必须且足以

\[
\lambda\ge\max\{0,\ 2(1-\epsilon)^{1/(n-1)}-1\}.
\]

在固定 n、δ=1−λ 很小时，误差为 (n−1)δ/2+O(δ²)。这只是声明的独立退相干模型中的精确公式与展开，不是从认知原则推导出的噪声规律。

若噪声浴也重新开放，其相干或测量信息可能支持进一步恢复，以上后处理界不适用。若提前添加冗余编码，也改变了噪声任务与资源。整体仍在和恢复器能否访问仍须分别记账。

## 6 复算

- 脚本：[coherent_history_noise_bound.py](coherent_history_noise_bound.py)。
- 结果：[coherent_history_noise_bound_results.json](coherent_history_noise_bound_results.json)。
- 8 项检查覆盖精确相位权重、实际原门浴耦合、正交反射、任意复恢复探针、达到见证、承诺边缘不变、取向块衰减与外部误差。

一般最优性由 Bessel 不等式和达到通道证明。下一步处理另一个问题：在固定最优共同接口下，能否仅用最少相干位配合经典标签，保留完整恢复能力。
