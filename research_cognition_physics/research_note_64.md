# 第六十四轮：用有限次旧指针读取高精度对齐独立实来源

日期：2026-09-15。接续第 62—63 轮。

## 1 结果：无需理想奇偶仪器，也无需丢弃分支

对两份完美但独立的旧实参考，Bob 的一次原 YX 门把相对方向写到 B_R 的 Z 上。随后借助一个可重置的本地辅助指针，重复复制、读取和重置，最后按多数结果对齐，就能把三方参考的迹距离误差压到任意小。

| 原指针读取次数 m | 三方参考迹距离的诊断值 | 证明形式 |
|:---|:---|:---|
| 1 | 0.005192081491 | 原单次仪器 |
| 3 | 0.00008059319737 | 有限完整电路与有理尾概率 |
| 9 | 4.672473398×10⁻¹⁰ | 严格有理上界小于 4.673×10⁻¹⁰ |
| 15 | 3.290183264×10⁻¹⁵ | 严格有理上界小于 3.3×10⁻¹⁵ |

两个多数结果都使用，不存在只挑成功样本的隐含步骤。有限误差严格为正；理想 τ₃ 仅进入精度闭包。

## 2 为什么原地多读几遍不是同一个方案

原 η=1 的 fresh Z 仪器并非保持输入 Z 取值的噪声传感器。取得结果 s 后，保留下来的原系统成为对应的 Z 态；后续读取已在读这个新态。给定第一条记录后，后续轨迹不再携带最初 Z 标签的额外信息。

于是初始 Z=±1 对全部顺序记录的总变差仍只有 α。代码直接用原 Kraus 更新计算 1、3、5 次完整记录并核对此值。简单重读原系统不能产生本轮表格中的放大。

正确做法是第 48 轮的辅助复制：把 B_R 的 Z 取值写入独立重置为 |0〉的 D，读 D 并丢弃，再重置 D。原 B_R 的 Z 取值保持不变。

## 3 原门如何实现保持式读取

Bob 首先仅执行一次 U_YX(π/2) 作用于 B_L、B_R。在便于计算的方向分量 a,c 上，B_L 保持 Y=a，而 B_R 成为 Z=ac。对这个 Z 指针使用已编译的实复制门

\[
C=I_D\otimes|0\rangle\langle0|_{B_R}
  +(-iY)_D\otimes|1\rangle\langle1|_{B_R}.
\]

它由一次原 YX 流及原局部实旋转组成。D 的一次结果 o 对原指针的效果为二元噪声通道

\[
p(o\mid t)=\tfrac12(1+\alpha ot),\qquad t=ac.
\]

独立重置和原 fresh 噪声规则使给定 t 的各次读取独立。程序在完整四系统源态上逐次加入第五个系统 D，执行编译门、完整 Kraus 读取，再部分迹丢弃 D；没有直接拿二项模型代替实际电路。

## 4 全部路径及多数结果的状态

对奇数 m 和记录 h=(o₁,…,o_m)，两个相对方向的似然为

\[
L_\pm(h)=\prod_j\frac{1\pm\alpha o_j}{2}.
\]

对齐前的非归一化保留态精确为

\[
\widetilde\rho_h=\tfrac12L_+(h)\rho_+(\gamma=1)
                    +\tfrac12L_-(h)\rho_-(\gamma=1).
\]

多数标记 r=sign(Σo_j)。如 r=−1，则使用第 62 轮带辅助的实 X 修正；所有路径求和后，得到

\[
\rho^{(m)}_{\rm aligned}
=\frac18[III+YYI+\gamma_m(IYY+YIY)],
\quad\gamma_m=1-2\delta_m,
\]

其中

\[
\delta_m=\sum_{k=(m+1)/2}^m {m\choose k}
\left(\frac{1-\alpha}{2}\right)^k
\left(\frac{1+\alpha}{2}\right)^{m-k}.
\]

与 τ₃ 的迹距离恰为 δ_m，不需要额外除以成功率。在指定的独立二元指针记录中，多数是等先验最优鉴别，故也是该记录模型下最优的方向修正。这里没有证明它对全部可能的共同仪器或门预算全局最优。

## 5 严格精度与完整资源账

复用第 48 轮的 100 位整数区间及二项尾概率证书，15 次的严格上界为

\[
\delta_{15}\le
\frac{4170802789592989}{1267650600228229401496703205376}
<3.3\times10^{-15}.
\]

下界也严格大于零，避免浮点输出“等于纯参考”的误读。

每次三方参考准备需要：

- 两个独立的旧实参考对，不能把它们算成一个免费公共源；
- Bob 一次初始对齐流，以及 m 次复制流和 m 次原读取；
- Bob 一个可反复重置的额外辅助系统；
- 若要求实际参考已对齐，最多一个结果比特发往 Charlie，Charlie 最多使用一次辅助修正流；
- 不丢弃任何结果分支，结束后丢弃 B_R。

若只做第 63 轮的 Pauli 层析，可在最终分析中使用多数标记，无需提前做 Charlie 的物理修正。若把每个独立来源的参考准备也按第 60 轮记账，则再计两次原流。门段编译误差和非理想辅助重置未混入 δ_m；本文沿用已声明的精确原流与独立实重置。若源自身 u、v 不完美，第 62 轮公式表明仍有来源误差，不能仅靠重复指针把它们一起消掉。

## 6 复核和下一步

实现：[amplified_source_alignment.py](amplified_source_alignment.py)。结果：[amplified_source_alignment_results.json](amplified_source_alignment_results.json)。新增 **9 项检查通过**，包含完整路径、保持式复制、原地重读反例、无筛选质量、多数最优决策及严格正误差证书。

```text
python -X utf8 research_cognition_physics/amplified_source_alignment.py --write-results
```

噪声问题已经在明确资源下解决到任意精度。下一轮转向另一问题：即使知道相对方向，若测量已经做完，能否对任意设置只靠经典记录处理补救？该问题不等同于参考是否足够纯。
