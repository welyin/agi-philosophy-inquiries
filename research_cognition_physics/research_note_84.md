# 第八十四轮：保留旧能力，并严格扩大认知边界

日期：2026-09-15。用户补充：组合后的认知系统不只应复现原结构，还应进一步扩大认知边界。按“仅供参考”的语气，将它列为待检验原则，不当作已经成立的物理公理。

## 1 把要求变成两项可核对条件

**保留**：每个旧协议在组合系统中都有对应实现，对嵌入的旧准备给出相同完整记录分布。以矩阵候选为例：

\[
\rho\mapsto\rho\otimes\sigma,\quad
K\mapsto K\otimes I,\quad E\mapsto E\otimes I.
\]

σ 是声明的伙伴准备。逐分支有 $(K\otimes I)(\rho\otimes\sigma)(K^\dagger\otimes I)=K\rho K^\dagger\otimes\sigma$，故任意有限自适应旧协议可以照常执行。这里保留的是可调用能力，不要求任意一次新的相互作用都不改变旧状态。

**扩展**：存在一个明确任务、输入分布和资源条件，组合允许的协议取得严格更好的成绩。记任务价值为

\[
V(\mathcal P)=\sup_{\pi\in\mathcal P}\Pr(\text{任务完成}\mid\pi).
\]

若旧协议可嵌入新协议，则至少不退步；若某个任务满足 $V(\mathcal P_{\rm new})>V(\mathcal P_{\rm old})$，便给出边界扩展的证据。

暂不要求每一次任意组合都有严格收益：完全重复的记录、没有通信或操作权限的伙伴都可能不增加能力。也不把扩展只定义为新增状态坐标；同样的信息可辨认性下，有限样本、可执行操作或正确率仍能改善。

这一形式与 SoCA 的完整小主体、临时组队及能力回归相呼应，见 [原工程方案](../认知联合体架构-AGI工程方案.md)；但它仍是本轮提出的有限验收条件，不代表已实现全部认知。

## 2 一个公平且严格的有限任务

输入是均匀随机选择的四种 Bell 态之一，只提供一份，目标是猜出完整标签。

基准允许：两方各自任意局部量子操作和经典通信，独立局部辅助，不额外提供共享纠缠资源。甚至给予理想本地读取，这一基准仍满足

\[
P_{\rm succ}^{\rm LOCC}\le\frac12.
\]

证明：每个有限局部通信协议的判决效应 E_b 为正乘积效应的和。任意 Bell 态对纯乘积向量的平方重叠最多为 1/2，谱分解后得到

\[
\operatorname{Tr}(\rho_b E_b)\le\tfrac12\operatorname{Tr}(E_b).
\]

于是

\[
P_{\rm succ}=\tfrac14\sum_b\operatorname{Tr}(\rho_bE_b)
\le\tfrac18\operatorname{Tr}I_4=\tfrac12.
\]

理想本地 Z 读取辨认一组奇偶类别，再随机猜另一个符号，可达到 1/2。原噪声基准也不可能超过这个更宽松的上界。

这属于已有 Bell 局部辨认结果；本轮读取 [Bandyopadhyay 等，Limitations on separable measurements by convex optimization](https://arxiv.org/pdf/1408.6981) 的开头与四 Bell 态段落，当前无额外共享资源的上界按以上两步直接证明，没有复现文中一般辅助纠缠优化。

## 3 只用已有实共同门即可严格跨过边界

组合协议增加一个原 YX Bell 逆门，将四个标签变成两个 Z 比特，再对每个比特使用 m 次旧指针读取。多数读取可见度为 $\gamma_m$，两个比特独立解码，所以

\[
P_{\rm succ}^{\rm joint}=\left(\frac{1+\gamma_m}{2}\right)^2.
\]

全 16 项输入—输出概率核对如下：若输出与真标签相差 h 个比特，

\[
P(\widehat b\mid b)
=\left(\frac{1+\gamma_m}{2}\right)^{2-h}
 \left(\frac{1-\gamma_m}{2}\right)^h.
\]

所有试验都有判决，不筛选结果。

| 每比特读取 m | 总旧读取数 | 成功率约为 |
|:---|:---|:---|
| 1 | 2 | 98.96427947% |
| 3 | 6 | 99.98388201% |
| 5 | 10 | 99.99972224% |

m=1 已有严格证书

\[
P_{\rm succ}\ge
\frac{1254521282748872455953902714155}
{1267650600228229401496703205376}>0.98.
\]

它使用一个旧共同逆门、两次原读取，无需 Y 新资源。m>1 另需旧保持式复制门与独立重置，不能把“一个 Bell 逆门”误读成整段程序仅有一个门。输入仍只有一份未知 Bell 态，指针重复不是新增未知态样本。

## 4 这条原则目前排除了什么

它证明“保留旧能力 + 有严格任务增益”并非空话。但所有 Bell 态、共同逆门和这套读取都可以完全为实，因此这项要求本身不选择复数域。

经典系统相对于单独观察者也能通过协作扩大范围：两人各拿一个均匀比特，各自不知道两位同异，交换结果后可以判断。这一较弱例子的基准只限单人，不能拿它冒充上面的“全部局部操作加通信”基准。

另一个提醒是：复两系统的局部联合统计原已完整，却仍存在上述单份辨认增益。所以只用“新增隐藏坐标”来定义认知扩展，反而漏掉复模型中的实际能力增长。

## 5 交付与后续

[cognitive_boundary_expansion.py](cognitive_boundary_expansion.py)；[结果 JSON](cognitive_boundary_expansion_results.json)。新增 8 项检查覆盖旧协议嵌入、经典协作对照、Bell 乘积重叠界、全部噪声记录、实际旧指针电路及严格成功率。没有采集现实实验数据，未从认知推出量子规则。

    python -X utf8 research_cognition_physics/cognitive_boundary_expansion.py --write-results

下一步沿第 83 轮继续：若整体还带有共同取向结构，保留它怎样形成可递归使用的接口？需要把该结构的输入假设和由它获得的能力分开。
