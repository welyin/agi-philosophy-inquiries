# 第一百二十八轮：确定性接入的最坏误差恰为 1/6

日期：2026-09-16。第 127 轮构造了误差 1/6 的确定性接入。本轮排除任意更准确的通用通道，包含不先测取向、任意联合控制及固定独立辅助资源的方案。

**定理：两个独立的未知两量子位标准实编码，确定性输出共同编码的最优最坏迹距离误差为 1/6。** 同一值也适用于任意维旧群体与一个两能级新主体。

## 1 优化对象没有被缩成某种修复器

对任意实完全正保迹映射 Φ，定义

\[
\epsilon(\Phi)=\sup_{\rho,\sigma}
D\!\left(\Phi(\mathcal E(\rho)\otimes\mathcal E(\sigma)),
\mathcal E(\rho\otimes\sigma)\right).
\]

输入各一份，不提供未知态描述或副本。任意固定独立辅助态、全部分支反馈和固定共同输出解码，都可以并入 Φ。Φ 的输出可以是任意 8 维实态，不要求它先具有共同取向对称性；这使下界覆盖面更宽。

量词是“同一确定性通道，对所有未知输入”。某个已知输入、某个受限准备集可以精确接入，不与此定理冲突。

## 2 用 36 个输入就能证明必要误差

取六个纯态

\[
\mathcal Q=\{(I\pm X)/2,(I\pm Y)/2,(I\pm Z)/2\}.
\]

对每对 $a,b\in\mathcal Q$，定义实际输入和目标支撑效应

\[
\Omega_{ab}=\mathcal E(a)\otimes\mathcal E(b),\qquad
F_{ab}=\operatorname{real\_lift}(a\otimes b).
\]

$F_{ab}^2=F_{ab}$，秩为 2。它是效应，不是归一化密度矩阵；理想编码通过它的概率为 1。因而

\[
D(\Phi(\Omega_{ab}),\mathcal E(a\otimes b))
\ge1-\operatorname{tr}(F_{ab}\Phi(\Omega_{ab})).
\]

只需证明 36 个输入上的平均通过率不超过 5/6，就得到至少一个输入误差≥1/6。

六态还满足精确恒等式 $\sum_{a\in\mathcal Q}a\otimes a=I+\operatorname{SWAP}$，但下文直接使用有限矩阵，不需要随机采样逼近连续积分。

## 3 一个适用于全部通道的对偶上界

采用“输入、输出”顺序的 Choi 矩阵

\[
C_\Phi=\sum_r\operatorname{vec}(L_r)\operatorname{vec}(L_r)^{\mathsf T},
\quad C_\Phi\ge0,\quad\operatorname{tr}_{\rm out}C_\Phi=I_{16}.
\]

这里 vec 按输入索引再输出索引排列。定义

\[
R=\frac1{36}\sum_{a,b}\Omega_{ab}^{\mathsf T}\otimes F_{ab}.
\]

平均通过率就是 $\operatorname{tr}(C_\Phi R)$。若找到输入上的对称矩阵 Y 满足

\[
Y\otimes I_8-R\ge0,
\]

则每个允许通道都满足

\[
\operatorname{tr}(C_\Phi R)\le
\operatorname{tr}(C_\Phi(Y\otimes I_8))
=\operatorname{tr}Y.
\]

取第 126 轮的输入对齐投影 P（包括目标恒等因子），有 rank(P)=8。设

\[
\boxed{Y=P/16+(I-P)/24,\qquad \operatorname{tr}Y=5/6.}
\]

剩下唯一需要认证的条件是上述矩阵正性。

## 4 完全整数的正性证书

令第 126 轮整数矩阵 $C=\sqrt2K_+$，于是 $C^{\mathsf T}C=2P$。不含无理数地构造

\[
576Y=24I+6C^{\mathsf T}C,
\]

\[
576R=16\sum_{a,b}\Omega_{ab}^{\mathsf T}\otimes F_{ab}.
\]

第二式虽然由小的二进制分数构成，相加后全部为整数。代码在转整数前精确核对这一点，不使用舍入容差。

写

\[
M=576(Y\otimes I_8-R).
\]

M 是 128×128 实对称整数矩阵，逐项整数乘法验证

\[
\boxed{M(M-16I)(M-20I)(M-24I)(M-32I)(M-36I)=0.}
\]

因此 M 的每个实特征值属于 {0,16,20,24,32,36}，全部非负，正性得到证明。不是凭有限精度最小特征值接近零来判断。

代码同时认证各乘法中间和的绝对值不超过 **2,997,485,568**，远小于有符号 64 位整数上限，故整数消去没有溢出。$576\operatorname{tr}Y=480$，得到严格有理上界 480/576=5/6。

这是一份可复算的有限代数证书。构造中的直观取向分解有助于找到 Y，最终证明不假设被优化通道采用该分解。

## 5 锐界及更大旧群体

综上，

\[
\epsilon(\Phi)\ge1-\frac56=\frac16
\quad\text{对每个确定性实通道成立}.
\]

第 127 轮对任意混合或纯输入给出≤1/6 的构造，因此

\[
\boxed{\inf_\Phi\epsilon(\Phi)=1/6.}
\]

该构造还精确保留旧群体，所以在加入“旧群体必须精确保留”条件后，此最优值仍然可达。

若旧群体维数 $d_A>2$，在它的已知二维子空间中嵌入任意两量子位输入即可继承下界：若更大系统有误差<1/6 的通道，先作这个实嵌入，最后用迹保持通道把 A 压回该二维空间，会给出两量子位反例。压回通道可取保留子空间的 $V^\dagger$，以及把补空间各基向量送往固定 $|0\rangle$ 的 Kraus 算子；它对目标态精确，迹距离不增加。第 127 轮对任意 $d_A$ 又达到≤1/6，故两边夹住相同值。

本轮没有声称新主体维数大于 2 时已经求得一般最优，也没有解决多个独立新主体同时近似接入。

## 6 与“整体保留”的关系

全部环境与记录可以留在整体。即使先进行可逆大系统操作，再允许访问全部环境，只要最后确定性提供同一个指定共同输出，该过程仍是 Φ，仍受下界限制。

这不禁止第 125 轮保留标记后的旧局部接口。允许随具体查询切换解释和访问方式，与要求有一个统一共同状态供所有后续查询使用，是两个不同验收条件。

1/6 是当前单份、标准编码、任意未知输入的通用误差门槛，不能称为所有实量子模型的不可消除物理噪声。改变准备集合、继承共同参考或提供输入相关资源后，需要重新求界。

## 7 复核

[脚本](approximate_joining_optimality.py)；[整数证书与结果](approximate_joining_optimality_results.json)。8 项检查包含完整 Choi 归一、直接概率与 Choi 得分对应、达到对偶界、36 准备的迹距离、任意实随机通道交叉核对、效应归一和精确整数矩阵恒等式。随机通道只作实现检查，普遍界来自对偶正性证明。

    python -X utf8 research_cognition_physics/approximate_joining_optimality.py --write-results

已有近似转置通道的出处和适用范围见[第 127 轮](research_note_127.md)；这里的整体接入界有独立证书，不以单系统最优性代替证明，未声称文献优先权。

接续：[第 129 轮](research_note_129.md)把联合误差转化为双方原有状态和能力的最优分担关系。

三轮合并后，目录全部 **1226 项检查通过**，耗时 143.341 秒；旧脚本与旧结果保留。
