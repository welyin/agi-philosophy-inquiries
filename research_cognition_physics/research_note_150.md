# 第一百五十轮：两位消息恢复未知关系，精确计算方案误差

日期：2026-09-16。回到标准独立编码输入与任意未知外部关联。

**结果：一个相干历史位，加四类、固定长度两位的消息，可以保持原共同接口，并以最坏迹距离 (1+√(25−16√2))/12≈0.2116932603 恢复原输入及其外部关联。这个数是本方案的精确最坏误差；所有两位消息方案的最优值仍未求出。**

## 1 合并哪几类操作

沿用第 149 轮从零开始的标签，按下表合并。此处 X、Y、Z 是原修正记录名称，下面显式矩阵全部为实数。

| 两位消息 | 原标签 | 消息概率 | 采用的修正中点 W_g |
|:---|:---|---:|:---|
| 00 | A_X、A_Z，即 0、2 | 1/3 | Z_R⊗H_A⊗I_B |
| 01 | B_X、B_Z，即 3、5 | 1/3 | I_R⊗I_A⊗H_B |
| 10 | A_Y，即 1 | 1/6 | Z_R⊗I_A⊗I_B |
| 11 | B_Y，即 4 | 1/6 | I |

其中 H=(Z+X)/√2。消息概率与输入无关。

每对待合并的修正都反对易，故

\[
W_0=(U_0+U_2)/\sqrt2,\qquad
W_1=(U_3+U_5)/\sqrt2
\]

是实正交矩阵。它们并非两操作的概率混合，而是一个明确的可逆操作。按消息 g 执行

\[
D_g=\begin{pmatrix}K_+\\W_gK_-\end{pmatrix}^{T}.
\]

恢复器不读取 Q，也不重新准备未知输入。细标签差异留在封闭环境；对它求偏迹只是描述访问范围。

## 2 将恢复通道化简

设 J_A、J_B 分别是在负取向八维空间的 A、B 上作用的实反对称矩阵 −iY。定义

\[
\widetilde J_A=K_-^TJ_AK_-,\qquad
\widetilde J_B=K_-^TJ_BK_-,\qquad
L=P_++P_-/\sqrt2.
\]

一对修正经过中点逆操作后，负取向块是 (I±J)/√2。对两分支求和消去正负交叉项。加上两条单独修正，得到

\[
\boxed{
\mathcal R_4(\rho)
=\frac13\rho+\frac23L\rho L
+\frac16\widetilde J_A\rho\widetilde J_A^T
+\frac16\widetilde J_B\rho\widetilde J_B^T.}
\]

这是迹保持的实通道。此式对任意输入都成立；下一节的精确误差使用输入约束。

## 3 对任意未知外部纯化求精确误差

实际上只需 Ω 实对称且 Tr(P_±Ω)=1/2，比独立编码条件更宽。取 Ω 的任意完整纯化 |ψ⟩，令

\[
|\psi_\pm\rangle=(P_\pm\otimes I)|\psi\rangle,\qquad
\|\psi_\pm\|^2=1/2.
\]

实对称矩阵与实反对称矩阵的迹内积为零，所以

\[
\langle\psi_-|(\widetilde J_j\otimes I)|\psi\rangle
=\operatorname{Tr}(\Omega\widetilde J_j)=0.
\]

与 ψ_+ 的内积也为零，因为 J̃_j 只支撑在负取向。于是两个正误差项都在 span{ψ_+,ψ_-} 的正交补；它们的总迹为 1/6。两误差向量彼此不必正交，这不影响其和为半正定且迹为 1/6。

在归一化基 √2ψ_+、√2ψ_- 中，输出减输入的剩余块为

\[
B=\begin{pmatrix}
0&-(2-\sqrt2)/6\\
-(2-\sqrt2)/6&-1/6
\end{pmatrix}.
\]

这个块有一正一负两个本征值，迹范数为 √(25−16√2)/6。因此

\[
\boxed{D((\mathcal R_4\otimes\mathrm{id})(\psi),\psi)
=\delta_4=\frac{1+\sqrt{25-16\sqrt2}}{12}
\approx0.2116932602865452.}
\]

每一个满足条件的完整纯化都达到同一个数，无需知道其状态。对任意混合外部扩展，先进一步纯化再取偏迹；迹距离收缩保证 D≤δ₄。故该方案在原任务上的最坏误差**恰为** δ₄，不是对有限随机样本的外推。

第 149 轮的负取向见证仍使本方案误差达到 1/3，它不满足本节输入约束。两轮结果相容。

## 4 旧接口与原门

环境重组与标签合并不作用于旧共同接口，忽略环境后的平均状态仍是原最优平分接入器输出。按消息恢复开始以后才使用该接口，未承诺同时提供一个额外共同输出副本。

控制 H 可由目标 Ry(−π/4)、受控 Z、目标 Ry(π/4) 三段实现。加上必要的参考修正及原取向重编码的逆操作，四类条件恢复成本分别为

| 消息 | YX | Ry | 总门数 |
|:---|---:|---:|---:|
| 00 | 16 | 17 | 33 |
| 01 | 11 | 12 | 23 |
| 10 | 11 | 10 | 21 |
| 11 | 6 | 5 | 11 |

无新纯辅助，全部为原实门。本轮标签读取暂按理想；下一轮直接把两位类别写到两个可读位置，剩余细标签封闭，避免先读三位再把结果压成两位的漏计。

## 5 复算与结论范围

- [复算脚本](centered_history_recovery.py)与[结果](centered_history_recovery_results.json)：8 项检查，包含真实独立编码纯化、复外部等距变换、原门及解析块核对。
- 根式采用 100 位整数有理包络；浮点数只作显示。
- δ₄ 是**所构造解码器**的锐值，不是全部两位消息、全部环境仪器的共同下界。
- 单纯提高读取精度不能消去此方案合并细标签的误差；需要改变消息或恢复结构。
- 这给出了“审计精度可以按未来任务选取”的条件模型，尚不证明认知原则必然选择某种数域。
