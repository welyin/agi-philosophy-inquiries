# 第五十七轮：条件性补齐复两系统状态、控制与局部层析

日期：2026-09-15。接续第 54—56 轮的弱 Y 读取、提纯与实辅助控制。

## 1 本轮完成的条件性结构

沿用已声明的 Hermitian 矩阵、通常张量积与迹概率规则，保留原实共同操作。再要求第 54 轮任意非零新增 Y 读取、条件准备和独立重复资源，以及状态与控制的极限闭包。

在这些前提下，现在可以逐项证明：

- 单系统包含完整复 Bloch 球，并有完整复旋转控制闭包；
- 两系统连续控制闭包为 **SU(4)**，忽略不影响状态的整体相位；
- 联合准备闭包包含全部复 `4×4` 密度矩阵；
- 原 X、Z 噪声读取加任意非零新 Y 读取，已足以通过局部共同记录重建联合状态。

因此这条具体候选扩展已经补齐复两系统的状态、可逆控制与局部层析。**这仍是条件性完成，不能改写成从原认知原则推导了量子力学。** 本轮没有推出哈密顿量、物理时间、普朗克常数、时空或一般场论。

## 2 由一个旧相互作用得到全部十五个控制方向

第 56 轮的实辅助提升提供任意局部复旋转。两边分别作用在原生成元 YX 上，

\[
(U_A\otimes U_B)(Y\otimes X)(U_A^\dagger\otimes U_B^\dagger)
=(U_AYU_A^\dagger)\otimes(U_BXU_B^\dagger).
\]

局部旋转可以分别将 Y 和 X 送到任一 Pauli 轴，所以原共同流的局部共轭提供全部九个 `P⊗Q` 相互作用方向，P、Q∈{X,Y,Z}。再加六个局部方向，恰好得到所有十五个非恒等 Pauli 字符串。

它们均为无迹 Hermitian 矩阵，且

\[
\operatorname{Tr}(P_jP_k)/4=\delta_{jk}.
\]

所以线性独立并张成 su(4) 的 Hermitian 生成元表示。连续控制群的闭包含全部 Lie 代数，故为 SU(4)。程序用整数 Pauli 矩阵取得单位 Gram 矩阵证书，并逐个检查九种实际局部共轭。

这是控制闭包结论；若只可调用原始有限门段，需要分配编译误差，不能将上述十五个方向直接等同于原始十五次调用。

## 3 任意复联合纯态的更直接准备方法

其实不必先编译任意 SU(4) 门才能准备全部纯态。把目标四维向量的振幅整理为 `2×2` 复矩阵 C，并作奇异值分解

\[
C=L\begin{pmatrix}s_0&0\\0&s_1\end{pmatrix}V^\dagger,
\qquad s_0^2+s_1^2=1.
\]

令 `θ=atan2(s₁,s₀)`。原实门已经满足

\[
U_{YX}(2\theta)|00\rangle=s_0|00\rangle+s_1|11\rangle.
\]

再对两边分别执行 L、`(V†)ᵀ`，振幅矩阵便成为 C。这就是完整构造：

**一次原实共同演化 + 两次局部复旋转。**

两次局部旋转可以依次使用同一个第 56 轮 Y 辅助资源，在 R–A、R–B 两对上分别执行实 SO(4) 提升；纯资源最终无相关归还。有限偏置 ν 时，目标输出是

\[
\frac{1+\nu}{2}|\psi\rangle\langle\psi|
+\frac{1-\nu}{2}|\psi^*\rangle\langle\psi^*|,
\]

故误差至多 `(1−ν)/2`。第 55 轮可以任意逼近 ν=1。因此在准备闭包中，全部复联合纯态可达；再按谱分解进行经典混合，得到全部复联合密度矩阵。

这个构造只调用三系统上的两系统门，没有假设一个现成的任意三系统共同门。

## 4 从十个实状态参数到十六个复状态参数

原实联合基底是含偶数个 Y 的十个 Pauli 字符串；现在补出六个含奇数个 Y 的方向：

```text
IY XY YI YX YZ ZY
```

全部十六个 `I,X,Y,Z` 的张量积恰好张成复 Hermitian `4×4` 矩阵空间。归一化固定 II 坐标后，状态有十五个自由参数。

这里的十六和十五仍是状态参数计数。十五个控制生成元与十五个归一化状态参数在此数量相同，但描述的是不同对象；二者都不是时空维数。

## 5 局部层析现在由实际带噪声统计实现

对 P、Q∈{X,Y,Z}，使用局部效应

\[
E_s(P)=\frac12(I+s\beta_P P),\quad
\beta_X=\beta_Z=\alpha,\quad\beta_Y=\nu_0>0.
\]

两边选择九种轴组合之一，保留同次实验的两个结果。其记录满足

\[
p(s,t|P,Q)=\frac14[1+s\beta_Pa_P+t\beta_Qb_Q
+st\beta_P\beta_QT_{PQ}].
\]

由边缘均值除以 β_P 或 β_Q，得到六个局部坐标；由结果乘积均值除以 β_Pβ_Q，得到九个相关坐标。于是

\[
\rho=\frac14\left[II+\sum_Pa_PPI+\sum_Qb_QIQ+\sum_{P,Q}T_{PQ}PQ\right].
\]

这给出了必要的全部十五个归一化参数。**新 Y 读取可以很弱，理想读取并非层析前提。** ν₀ 变小时估计方差会增加；总体概率可反演不等于有限样本精确已知。

程序直接生成全部九种设置的四结果概率，再独立反演随机复密度矩阵，另检查无信号边缘。

## 6 一个能看到新增方向的例子

两态

\[
|\Phi_{\pm i}\rangle=\frac{|00\rangle\pm i|11\rangle}{\sqrt2}
\]

在全部旧实联合效应上给出相同统计，但它们的 XY 相关分别为 ±1。现在 A 用原 X 读取，B 用弱新 Y 读取，便得到总变差距离 `αν₀`。

当 ν₀=0.2 时，该值约 0.197923167。这是当前扩展候选的可区分预测，不是现实实验数据。

## 7 四轮链条，以及不能省略的逻辑缺口

| 阶段 | 已证明的作用 | 尚须作为输入的条件 |
|:---|:---|:---|
| 54 | 新 Y 效应迫使非实条件态，一次使用得到精确椭球 | Hermitian/迹概率扩展、新效应、条件准备 |
| 55 | 原噪声实协议可将任意非零资源偏置提纯到纯 Y 极限 | 同方向独立重复准备、通常张量组合、条件筛选 |
| 56 | 纯资源将复单系统门提升为实 SO(4) 门；强 Y 读取进入闭包 | 原实控制与辅助重置、极限闭包 |
| 57 | 旧相互作用补齐 SU(4)、全部复联合准备与局部层析 | 两边可用相同资源和局部操作 |

ν₀=0 时，实 Kraus 操作不能从实输入生成非实状态；ν₀>0 时，上述无限精度闭包包含完整复结构。这不代表一个有限实验的预测在 ν₀=0 处突然不连续：任意固定有限流程的概率仍连续，逼近完整结构所需资源会随初始灵敏度变弱而增加。

最重要的剩余问题是**为什么认知原则必须要求 ν₀>0，而不允许第 50—53 轮已自洽的实候选**。仅说要有预测闭包、角色对称或联合校准，尚不能排除 ν₀=0。

**下一轮入口**：将实候选与上述复扩展放进同一套认知验收条件中，逐项比较闭环预测、混合一致性、角色对称、无信号与联合校准。审查“联合区别必须能由各主体分别读取”是否只是局部层析的重述，或能否从不预装量子结构的独立认知要求推出。完整矩阵重建的条件后果已明确，下一步重点转向新增前提本身。

## 8 文献与复核

本轮核对 [Brylinski 与 Brylinski，Universal quantum gates](https://arxiv.org/abs/quant-ph/0108062) 的摘要与版本，确认局部门加纠缠门的通用性是已有主题；没有声称复现其全部一般维数证明。当前两系统结论直接用十五个 Pauli 方向和上面的振幅矩阵分解证明。第 56 轮的辅助实模拟来源仍见其已列原文，不将这些标准矩阵方法称为原创量子重建。

实现：[complex_bipartite_closure.py](complex_bipartite_closure.py)。结果：[complex_bipartite_closure_results.json](complex_bipartite_closure_results.json)。

```text
python -X utf8 research_cognition_physics/complex_bipartite_closure.py --write-results
python -X utf8 -m unittest discover -s research_cognition_physics -p "*.py"
```

新增 **10 项检查通过**，包括精确十六维基底、十五维控制 Gram 证书、九种局部共轭、全部纯态准备公式、共享参考的实际三系统电路、混合正态、弱读取层析、复 Bell 反例及无信号。Python 3.12.14、NumPy 2.3.5，无新增依赖，旧轮次代码与结果保留。

第 54—57 轮共新增 **43 项检查**；目录全部 **634 项检查通过**，完整回归耗时 107.958 秒。
