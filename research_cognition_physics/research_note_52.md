# 第五十二轮：局部等价操作不能直接互换，完整接口还缺一个标量

日期：2026-09-15。接续第 51 轮，从状态充分性转向操作的组合充分性。

## 1 本轮结果

在已声明的实 Kraus 操作与通常实张量组合中：

1. 对全部单系统输入及全部局部后续记录等价的两个操作，接入联合系统后仍可能给出不同概率。
2. 对单个实二能级系统，原来作用于三个状态坐标的 `3×3` 操作矩阵之外，**恰好还需一个操作标量 κ**。
3. 两操作的原矩阵和 κ 都相同，则它们与任意有限维实辅助系统组合后的作用相同；原矩阵相同而 κ 不同，则一个辅助实系统已经足以区分。

这给出了当前候选内完整的替换判据，没有把 κ 当成新增的单主体状态坐标，也没有因此推出复数状态空间。

## 2 把第 46 轮的扩展差异带回实联合态

令 `J=−iY`，它是实二维半圈生成矩阵。比较两个通道：

\[
\mathcal D_R(\rho)=\frac{1+\alpha}{2}\rho+
\frac{1-\alpha}{2}J\rho J^T,
\]

\[
\mathcal D_I(\rho)=\frac{1+3\alpha}{4}\rho+
\frac{1-\alpha}{4}(X\rho X+J\rho J^T+Z\rho Z).
\]

每一项都有实 Kraus 矩阵，且权重非负、总和为 1。第二个通道就是第 46 轮各向同性扩展的实 Kraus 表示；把单个 Kraus 矩阵乘整体相位，不改变通道。

两者都满足

\[
I\mapsto I,\quad X\mapsto\alpha X,\quad Z\mapsto\alpha Z.
\]

它们在整个实单系统矩阵空间上相同。因此插入任意局部旋转、读取、条件分支和经典控制，都不可能区分两者。第 46 轮完整 fresh 仪器中的两个扩展也逐分支相同，故这一等价包含全部局部自适应记录，而不只是一次平均输出。

但两者分别满足 `D_R(Y)=Y`、`D_I(Y)=αY`。Y 单独不是合法实状态方向，YY 却是合法联合方向。

将它们各自作用于第 47 轮已经可准备的 `ρ₊=(I₄+YY)/4` 的第一边，得到

\[
(\mathcal D_R\otimes\mathrm{id})(\rho_+)=\frac{I_4+YY}{4},\qquad
(\mathcal D_I\otimes\mathrm{id})(\rho_+)=\frac{I_4+\alpha YY}{4}.
\]

再执行同一个 `U=exp(−iπYX/4)`，读取第二边 Z，正结果概率分别为

\[
\frac{1+\alpha}{2},\qquad\frac{1+\alpha^2}{2}.
\]

可读的总变差差距为

\[
\boxed{\frac{\alpha(1-\alpha)}2\approx0.00513816607>\frac1{200}.}
\]

严格下界由 100 位整数区间认证。这不是新物理实验，而是两个已明确候选操作在同一允许协议中的可区分预测。

## 3 另一个通道是否只是形式上的扩展

上述两个通道都是合法实完全正映射。若只保留最初单系统原语，不能因此宣布所有这些 Kraus 实现都自动可用。

但在第 50 轮的完整实两系统控制、辅助准备与丢弃、经典随机混合条件下，第二个通道有直接实现：X、Z 的单系统反射可分别通过 `X_A⊗J_C`、`Z_A⊗J_C` 实现。这两个实 4×4 矩阵的行列式均为 +1，属于 SO(4)。对输入 `ρ_A⊗τ_C`，丢弃 C 后恰好得到 `XρX` 或 `ZρZ`。再按上式混合即可。

辅助 C 结束时可以改变；这与第 50 轮必须归还辅助的交换任务不同。该实现也不会把某个原始生成元的控制闭包误说成已给出有限精确编译。

## 4 恰好缺一个标量

对任意实 2×2 矩阵 K，直接展开得到

\[
\boxed{KJK^T=(\det K)J.}
\]

所以对任意实 Kraus 操作

\[
\Phi(M)=\sum_jK_jMK_j^T
\]

都有

\[
\boxed{\Phi(Y)=\kappa_\Phi Y,\qquad\kappa_\Phi=\sum_j\det K_j.}
\]

实对称输入仍映到实对称输出，实反对称输入仍映到实反对称输出；后二者的空间在二维只有一个方向。因此，在按 `(I,X,Z,Y)` 排列的矩阵基底上，操作恰好具有分块形式

\[
\widehat\Phi=\begin{pmatrix}B_\Phi&0\\0&\kappa_\Phi\end{pmatrix}.
\]

B 是原三个齐次状态坐标的线性作用，κ 是剩下的一个数。虽然求和公式使用 Kraus 矩阵，κ 也由 `Φ(Y)` 唯一确定，因此不依赖 Kraus 表示的选取。

若两操作 B 和 κ 一致，它们就在全部实矩阵上相同。与任意大小的恒等映射取张量积，仍逐基元素相同，故对任意联合输入相同。任何后续操作都不能重新产生区别。

若 B 相同、κ 不同，上节的 ρ₊ 探针及 YY 读取已经给出差异。这也证明新增的一个参数确实必要，不能只凭重新命名局部等价类把它删去。

## 5 如何读取和组合这个参数

对于一般选择性操作 Φ，作用于 ρ₊ 第一边后，不归一化地保存操作是否发生及最终 ± 读数。前述 YY 读取满足

\[
p_+-p_-=\alpha\kappa_\Phi.
\]

不先按成功条件归一化，便可直接估计 κ。若先归一化，还需保留分支总概率才能恢复它。

顺序执行与经典混合的规则分别为

\[
B_{\Psi\circ\Phi}=B_\Psi B_\Phi,\quad
\kappa_{\Psi\circ\Phi}=\kappa_\Psi\kappa_\Phi,
\]

\[
B_{p\Phi+(1-p)\Psi}=pB_\Phi+(1-p)B_\Psi,\quad
\kappa_{p\Phi+(1-p)\Psi}=p\kappa_\Phi+(1-p)\kappa_\Psi.
\]

κ 描述本地操作对联合相关方向的影响；它不是本地密度矩阵多出的一维。完整操作描述和单独可准备状态的描述可以有不同的信息要求。

## 6 认知原则不能在这里被偷换

标准化能力声明与接口可替换性，要求的是**相对于声明的全部使用场景**具有相同表现。只在单独使用时等价，不能直接算作任意组合场景下等价。

因此有两种不同研究选择：

- 把联合校准也纳入操作身份，此时 `(B,κ)` 修复了当前实候选的预测接口。
- 额外要求单独校准就能保证任意联合场景可替换；这是更强的物理原则，需要独立论证。

第一种选择已经实现了组合安全，并未迫使复量子理论成立。第二种原则是否有认知依据、是否足以在更一般候选中选出目标结构，尚未证明。

## 7 文献与复核

第 51 轮所读 Hardy–Wootters 论文 §3 的脚注明确提示：单独的操作等价是否能扩展到组合，需要说明上下文范围。本轮没有把它关于测量效应的脚注直接当成一般变换定理。

另读 [Leung，Choi’s Proof and Quantum Process Tomography](https://arxiv.org/pdf/quant-ph/0201119) §2—4，核对完全正映射、Kraus 表示和借助参考系统识别操作的框架。一般辅助过程层析是已有方法；本轮的任务是给出现有实接口的两通道反例与明确补充参数，未声称一般方法原创。

实现：[ancillary_operation_equivalence.py](ancillary_operation_equivalence.py)。结果：[ancillary_operation_equivalence_results.json](ancillary_operation_equivalence_results.json)。

```text
python -X utf8 research_cognition_physics/ancillary_operation_equivalence.py --write-results
```

新增 **9 项检查通过**，覆盖实 Kraus 表示、完整局部记录、联合探针、严格读数差距、辅助反射实现、整数行列式恒等式及任意辅助维数的解析判据的数值对照。运行时与依赖不变；旧代码与旧结果保留。

**下一轮入口**：求出与全部现有 fresh 分支兼容的 κ 精确区间，证明哪些补充参数真正允许完全正扩展，以及哪些局部操作已经没有扩展歧义。
