# 第一百零七轮：相关环境可实际准备，也可只用经典消息帮助恢复

日期：2026-09-16。

接续 [第 106 轮](research_note_106.md)。本轮补齐相关源的原实门准备，保留其纯化系统，并取消恢复阶段 E_0 与 M 的共同门，求环境先测量、单向发消息的能力。

## 1 六个纯槽的源准备

引入环境 E_0,E_1,E_2 和纯化系统 R_0,R_1,R_2，全部初始化为 |0〉。准备三对状态：

\[
|\psi_\kappa\rangle_{E_0R_0}
=\sqrt{\frac{1+\kappa}{2}}|00\rangle+
 \sqrt{\frac{1-\kappa}{2}}|11\rangle,
\]

其余两对为同式 κ=0 的状态。每对通过 E_i 上一次 R_y 旋转和一个原受控实复制门完成。E_i 边缘的张量积为

\[
\frac{I+\kappa Z_0}{2}\otimes\frac I2\otimes\frac I2.
\]

再在环境上施加

\[
T=R_{Z_1Y_2}(\pi/2)R_{Y_0X_1}(\pi/2).
\]

第一个门将 Z_0 变成 X_0X_1，第二个将它变成 X_0Y_1Y_2。因此 T Z_0 T†=XYY，恰好准备 σ_κ。

Z_1Y_2 旋转由 X_1Y_2 两侧的局部 Y 旋转得到，实际共 2 次两系统旋转、2 次局部旋转，明确使用第 50 轮的角色反向 XY 能力。含三对纯化的固定准备电路总共：

- 5 次两系统旋转；
- 14 次额外局部旋转；
- 6 个纯初始化量子槽。

κ 为端点时可以省掉某些空动作，但表中统一保留这套固定电路，未声称准备成本最小。

R_0,R_1,R_2 从未删除，也不开放给恢复者。取偏迹只计算当前接口输出。整个源、记忆及可能的旧复参考仍在同一个整体内。

## 2 不执行环境逆门，直接本地读取

记 λ_i=cos(θ_i/2)、s_i=√(1−λ_i²)，并令

\[
\zeta=\lambda_0\lambda_1\lambda_2,\qquad Q_\kappa=\kappa s_1s_2.
\]

第 106 轮先逆变换后的响应 C_0=(BI−Q_κX)/2。未逆变换时交叉块变成 C_0R_0†，其厄米部分为

\[
\frac12\left[\zeta I+
Q_\kappa(s_0Z-\lambda_0X)\right].
\]

因此只需在 E_0 上读取实轴

\[
\boxed{F=s_0Z-\lambda_0X.}
\]

环境多数消息 r 的概率为 1/2，与输入无关。使用 m 次原读取、γ=γ_m，保留全部分支后，M,N 的未归一化仪器为

\[
\boxed{
\mathcal J_r(\omega)=\frac12\Lambda_{\zeta+r\gamma Q_\kappa}(\omega),
}
\]
\[
\Lambda_t(\omega)=\frac{1+t}{2}\omega+\frac{1-t}{2}A\omega A,\qquad -1\le t\le1.
\]

相干系数 t 可以为负，它代表带相位反转的通道，仍完全正。条件查询在 |t|>c 时读关联轴并按 sign(t) 翻转答案，否则只读 N。最终 q 次原读取得到

\[
\boxed{
P_H=\frac12+\frac{\gamma_qw}{4}
\left[
\max(c,|\zeta+\gamma_mQ_\kappa|)
+\max(c,|\zeta-\gamma_mQ_\kappa|)
\right].
}
\]

环境消息自身不报告 H 真值。它改变的是该如何利用剩余记忆。

## 3 全部实单向环境测量的理想界

允许 E_0 先作任意实 POVM，再把结果发给 M,N；接收方可作任意后续判断，但不能先反馈或与 E_0 作共同量子门。

写任意非零实效应为 F_j=f_j(I+n_j·σ)，其中 n_j 位于 XZ 圆盘，Σf_j=1、Σf_j n_j=0。E_0 边缘为 I/2，故消息概率为 f_j。相关归一化相干系数为

\[
t_j=\zeta+Q_\kappa x_j,\qquad
x_j=(s_0\hat z-\lambda_0\hat x)\cdot n_j,\quad
|x_j|\le1,\quad\sum_j f_jx_j=0.
\]

函数 g(x)=max(c,|ζ+Q_κx|) 为凸函数。以两端连线控制：

\[
g(x)\le\frac{1+x}{2}g(1)+\frac{1-x}{2}g(-1).
\]

平均后，任何此类 POVM 的恢复因子至多 [g(1)+g(−1)]/2，由 F 轴的二结果投影达到。于是上一节 γ_m=1 的式子给出这个**环境先测量、单向实消息类**的精确理想界。

若固定二结果保护读取可见度 γ_m，则在所声明轴读取方案内同样由端点方向取到最大值。没有证明任意有限同资源仪器最优，也没有优化含 Y 效应的复环境测量、双向 LOCC 或其他量子协作。

## 4 访问限制与有限数值

c=d=4/5，环境和末端各读取一次：

| 门参数 | κ | 恢复阶段操作 | H 成功率 |
|:---|:---:|:---|:---:|
| 三个 λ_i=0.9 | 0 | 本地环境消息 | 73.7507800884% |
| 三个 λ_i=0.9 | 1 | 本地环境消息 | 75.4879568109% |
| 三个 λ_i=0.9 | 1 | 先逆 E_0–M 门，再发环境消息 | 76.6903400529% |
| 三个 λ_i=0 | 0 | 本地环境消息 | 73.7507800884% |
| 三个 λ_i=0 | 1 | 本地环境消息 | 79.3801851463% |

第一组 κ=1 的完全联合理想 H 区别为 0.54；直接单向实消息的理想区别为 0.5157。因此量子协作与本地经典消息之间也有明确差距，不能把所有访问方式混称为“拿回一个片段”。

第二组 λ_i=0、κ=1 时，本地理想消息已恢复 H 区别 w。环境保护读取三次、末端一次时，有限成功率为 79.6836897323%。这里片段本身没有非零 G 标签记录，不是在恢复两份不可访问的完美旧记录。

表中先逆门的实现确实额外使用一次 E_0–M 两系统旋转及五次局部旋转；它不是只通过经典消息返回的协议。

## 5 完整资源账

包含源纯化准备、旧 M,N 两次写入、三个环境记录门，以及最贵关联查询分支：

| 环境/末端读取次数 | 恢复方式 | 两系统旋转 | 额外局部旋转 | 新增量子槽峰值 | 重置 | 返回消息 |
|:---:|:---|:---:|:---:|:---:|:---:|:---:|
| 1/1 | 直接本地读取 | 13 | 40 | 8 | 0 | 1 位 |
| 1/1 | 先逆 E_0–M 门 | 14 | 45 | 8 | 0 | 1 位 |
| 3/1 | 直接本地读取 | 16 | 50 | 9 | 2 | 1 位 |
| 3/3 | 直接本地读取 | 19 | 59 | 10 | 4 | 1 位 |

基本八个槽为 E_0,E_1,E_2、三个 R 以及 M,N。纯化系统不可拿来充当已初始化本地指针。环境侧和记忆侧的保护指针各自计数。只读 N 的分支可以省去关联解码，表列统一上界。

共同源必须先通过声明的门准备；源准备所需的共同连接能力没有隐藏进“恢复阶段只发经典消息”。运输、路由、非理想门误差和实际能耗仍未计价。

## 6 验证及范围

[脚本](correlated_local_recovery.py)及[结果](correlated_local_recovery_results.json)新增 8 项检查，覆盖真实六槽纯化、T 的 Pauli 共轭、任意复参考的完整消息仪器、全部有限条件查询、实测量方向与解析界对照、旧无条件通道及资源账。

噪声 α 仍为原值，未增加理想读取原语；相位擦除与环境初态文献归属沿用第 102、106 轮。本轮仍在预设量子框架内做模型推导。

**下一步**：明确什么摘要足够预测所有允许的后续记录，并量化漏记关联参数的误差。
