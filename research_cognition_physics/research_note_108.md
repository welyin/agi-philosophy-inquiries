# 第一百零八轮：未来预测所需的环境摘要与精确误差

日期：2026-09-16。

接续 [第 107 轮](research_note_107.md)。本轮将结果接回认知方向的操作状态定义：若两个历史对所有允许的未来协议都给出相同记录，才将它们看作相同状态。当前某项最优成功率相同，还远远不够。

## 1 固定接口的完整响应摘要

条件固定为：环境与输入记忆初始独立；耦合门已知；之后只开放 S 与记忆；U 及源纯化系统始终不能再交互。沿用

\[
\sigma_S=\operatorname{tr}_U\sigma,\qquad
C_S=\operatorname{tr}_U[\sigma(I_S\otimes R_U^\dagger)].
\]

第 106 轮的完整输出 Ψ_(σ_S,C_S) 对任意输入、任意未操作参考成立，实际可访问输出只相差一个已知受控门。因此：

\[
\boxed{
(\sigma_S,C_S)\text{ 相同}
\quad\Longleftrightarrow\quad
\text{整个可访问输入输出通道相同}.
}
\]

充分性直接来自四块输出式。必要性也能操作检验：

1. 将 M 准备在 A 的正扇区，输出环境边缘直接给出 σ_S。
2. 再准备 M 的 L 正本征态。在 A 本征基中取输出的非对角块，它等于 C_S 的非零已知倍数；当前基约定下为 −C_S/2。

所以输入输出通道一致时，两者都必须一致。对于一般复源，这里的完整通道是全复矩阵意义；若只开放实效应，不能据此说所有复矩阵差异都可见。我们的显式源族、探针及响应均可取实，其区别由下面的实门协议读出。

这不是要求主体储存整个不可访问环境。只需保存与固定可干预接口相关的响应。若后来改变门、开放 U 或让 U 再次反馈，这个摘要可能不再足够；它不是已经证明的一般多时刻认知状态公理。

## 2 全局源不同，局部未来仍可以完全等价

考虑

\[
\sigma_{\kappa,\epsilon}
=\frac{I+\kappa XYY+\epsilon ZZZ}{8},
\qquad \kappa^2+\epsilon^2\le1.
\]

XYY 与 ZZZ 反对易且各自平方为 I，所以该条件保证正性。对只开放 E_0 的固定旋转接口，

\[
\operatorname{tr}_{12}[ZZZ(I\otimes R_1^\dagger\otimes R_2^\dagger)]=0.
\]

故 ε 不改变 σ_0、C_0。相同 κ、不同 ε 的两个源可以有显著全局区别，却在当前全部可访问通道上严格相同。

例如 κ=0.4、ε=±0.6，两个源的全局迹距离为 0.6，但任意输入与参考的可访问输出完全相同。当前接口没有理由把 ε 加入自己的预测状态。它仍保留在整体中，并非被物理删除。

相反，κ 改变 C_0，会改变某些未来记录。因此“把所有隐藏信息都忽略”和“把完整整体都存进局部模型”都不是必要规则；应按允许的未来操作检验摘要是否充分。

## 3 一般摘要误差界

比较两对物理可实现摘要，记差为 δσ、δC。对于任意包含参考的密度矩阵 ω，令

\[
\operatorname{Diag}_A(\omega)=P_+\omega P_++P_-\omega P_-.
\]

有 ‖Diag_A(ω)‖_1=1，且正矩阵的交叉块满足

\[
\|P_+\omega P_-\|_1
\le\sqrt{\operatorname{tr}(P_+\omega)\operatorname{tr}(P_-\omega)}
\le1/2.
\]

分别控制输出的对角项和非对角项，得到

\[
\boxed{
\sup_{\omega,\mathrm{reference}}
D(\Psi_1(\omega),\Psi_2(\omega))
\le\frac{\|\delta\sigma\|_1+\|\delta C\|_1}{2}.
}
\]

可再与平凡上界 1 取较小值。已知门共轭不改变这个界，任何后续协议的记录总变差也不超过它。该式给出充分误差预算，没有声称对任意摘要都达到等号。

## 4 当前 κ 族的锐界

对 σ_κ=(I+κXYY)/8，令 q=s_1s_2。此处 σ_0=I/2，δC=−δκ q X/2。输出差简化为

\[
\delta\Psi(\omega)
=-\frac{\delta\kappa q}{2}X_0\otimes
\operatorname{Off}_A(\omega),
\]
\[
\operatorname{Off}_A(\omega)=\frac{\omega-A\omega A}{2}.
\]

因为 ‖Off_A(ω)‖_1≤1，得到

\[
\boxed{
\sup_{\omega,\mathrm{reference}}
D(\Phi_{\kappa_1}(\omega),\Phi_{\kappa_2}(\omega))
=\frac{|\kappa_1-\kappa_2|s_1s_2}{2}.
}
\]

等号由 M 的纯 L 正本征态达到，无需纠缠辅助输入。此式包含任意复参考的最坏情况，不是只在 H 校准态上得到的差异。

例如 λ_1=λ_2=0.9，漏记 κ 从 0 到 1，会允许最高 0.095 的未来记录总变差。它不是“认知损失 9.5%”；它限定的是同一协议输出分布可相差多少。

若 κ 的校准误差保证不超过 e，在门参数已知的条件下，全部此接口预测误差最多为 e s_1s_2/2。建立 e 的有限样本置信保证仍需独立源样本，不能把同一份记忆的保护重读当作重新取样。

## 5 用原实门读出遗漏的有符号参数

独立执行校准探针：

- M 准备为 L 的正本征态；
- 施加同样三个环境耦合；
- 开放 E_0，并实际逆转一次 E_0–M 门；
- 读取 X_0⊗L_M。

最后的乘积轴用一个原受控实门加两次轴旋转解码到 E_0 的 Z，连同该门内部旋转共 1 次两系统旋转、5 次局部旋转。不是假设免费联合测量。

m 次原保护读取的多数符号 r 满足

\[
\boxed{\mathbb E[r]=-\gamma_m\kappa s_1s_2.}
\]

因此比较两候选 κ_1、κ_2 的完整读数历史，总变差为

\[
D_{\rm records}
=\gamma_m\frac{|\kappa_1-\kappa_2|s_1s_2}{2}.
\]

多数符号保留这次二候选判别的似然比符号，实际全部原始分支也已核对该等式。随着读取精度提高，达到上节理想通道区别。

本探针是另外一次校准实验，不执行原 AB 的 G、H 两次写入。包含六槽源准备、一个纯 M 探针及所有耦合，m=1 时需要 10 次两系统旋转、40 次额外局部旋转、7 个纯初始化槽、1 次原读取。m≥3 另加 m 次两系统旋转、3m 次局部旋转、一个本地指针与 m−1 次重置。此处 E_0,M 共同可控，不是只通过环境经典消息返回的协议。

## 6 相同最优能力数值仍不是相同操作状态

κ=+1 与 κ=−1 的最佳 H 区别相同，因为第 106 轮公式只含成对的 |B±κq|。但是对应的条件反馈和有符号记录不同。

当 λ_1=λ_2=0 时，两者都能恢复完整的理想 H 区别 w；上一节探针的可访问输出却互相正交，理想可完全区分。一次原读取已把等先验源辨认成功率提高到

\[
(1+\alpha)/2=99.4807918509\%.
\]

所以“记下每个任务能做到的最高正确率”不足以定义未来预测状态。至少还需保存不同消息、动作与结果之间的对应关系。这里 κ 的符号就是一个不能被能力分数替代的例子。

## 7 验证、范围与下一步

[脚本](environment_response_summary.py)及[结果](environment_response_summary_results.json)新增 8 项检查，覆盖不同全局源的响应等价、响应提取、任意复参考的误差界、达到锐界的纯探针、真实实解码与完整噪声历史，以及相同最优 H 分数但不同未来记录的反例。

这一步与研究方向第 3.2 节“按全部允许未来记录定义操作状态”直接对应，但矩阵、张量划分和门集仍来自预设量子框架；没有从认知原则推出这些结构，也没有选出实或复数域。

**下一主任务**：给每个片段加入可认证的非零旧 G 记录，在保持各片段记录和整个无环境记忆通道相同的条件下，检验隐藏关联能否继续改善 H 恢复，并求记录强度、关联幅度及恢复误差之间的限制。当前反例的单片段旧记录为零，这一限制必须明确保留。
